# 🛠 Лабораторная работа №3: Система контроля версий Git

## 📝 Описание задания
В рамках данной лабораторной работы были изучены основные принципы работы с децентрализованной системой контроля версий **Git**. 

Были отработаны навыки:
* Создания и переключения между ветками (`checkout`, `branch`).
* Слияния веток (`merge`) и перебазирования (`rebase`).
* Разрешения конфликтов слияния (merge conflicts).
* Работы с историей изменений (`log`, `reset`).

---

## ✅ Выполненные шаги

- [x] **Инициализация:** Установка Git и генерация репозитория скриптом.
- [x] **Базовая работа:** Создание коммитов с добавлением строк в файл `notes.txt`.
- [x] **Ветвление:** Работа в ветке `hotfix` и параллельные изменения в `main`.
- [x] **Анализ:** Сравнение версий коммитов с сохранением вывода в `diff.txt`.
- [x] **Откат изменений:** Использование `git reset --hard` для модификации истории.
- [x] **Rebase:** Перебазирование ветки `hotfix` на `main` (автоматическое разрешение).
- [x] **Слияние (Merge):** Слияние веток `feature1` и `feature2` в `main`.
- [x] **Конфликты:** Ручное разрешение конфликта содержимого при слиянии `feature2`.
- [x] **Финализация:** Сохранение графа истории в `history.txt` и публикация на GitHub.

---

## 💻 Полезные команды, использованные в работе

Ниже приведен список основных команд, которые применялись для выполнения заданий:

```bash
# Просмотр красивого графа истории
git log --oneline --graph --all

# Создание ветки и переход на нее
git checkout -b <branch_name>

# Сравнение коммитов
git diff HEAD~1 HEAD > diff.txt

# Жесткий откат к конкретному коммиту
git reset --hard <commit_hash>

# Перебазирование текущей ветки на main
git rebase main

# Слияние веток
git merge <branch_name>
